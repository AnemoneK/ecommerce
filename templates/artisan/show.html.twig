{% extends 'base.html.twig' %}

{% block title %}Artisan{% endblock %}

{% block body %}
    <h1>Artisan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artisan.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ artisan.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ artisan.lastName }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ artisan.address }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ artisan.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artisan_index') }}">back to list</a>

    <a href="{{ path('artisan_edit', {'id': artisan.id}) }}">edit</a>

    {{ include('artisan/_delete_form.html.twig') }}
{% endblock %}
